import SwiftUI

struct PurchaseOptionsView: View {
        VStack {
            Text("購買選項")
                .font(.title)
                .padding()
            
            Text("此功能尚未實現")
                .foregroundColor(.gray)
                .padding()
            
            Spacer()
        }
        .navigationTitle("購買選項")
    }
}
